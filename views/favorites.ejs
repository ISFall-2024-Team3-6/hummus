<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Favorites</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 20px;
      margin: 0;
    }
    .favorites-container {
      width: 80%;
      max-width: 600px;
      background-color: #ffffff;
      padding: 20px;
      border: 1px solid #ddd;
      border-radius: 5px;
      box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
      text-align: center;
    }
    .favorites-header {
      font-size: 1.5em;
      margin-bottom: 20px;
      color: #333;
    }
    .add-new-btn {
      padding: 10px 20px;
      font-size: 1em;
      background-color: #4CAF50;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }
    .add-new-btn:hover {
      background-color: #45a049;
    }
  </style>
</head>
<body>

<div class="favorites-container">
  <div class="favorites-header">Favorites</div>

    <h1 style="text-align: center;">Favorites</h1>
    <table>
      <thread>
        <tr>
          <th>Name</th>
          <th>Description</th>
          <th>Serving Size</th>
          <th>Retail Price</th>
          <th>Brand Name</th>
        </tr>
      </thread>
      <tbody>
        <% if (favoritehummus && favoritehummus.length > 0) { %>
          <% favoritehummus.forEach(row => { %>
            <tr>
              <td>
                <form action="/deleteFavorites/<%= row.hummusid%>" method="POST" style="display: inline;">
                  <button type="submit" onclick="return confirm('Are you sure you want to delete this record?');">Remove</button>
                </form>
              </td>
              <td><%= row.hummus_name %></td>
              <td><%= row.hummus_description %></td>
              <td><%= row.serving_size %></td>
              <td><%= row.retail_price %></td>
              <td><%= row.brand_name %></td>
            </tr>
            <% }) %>
            <% } else { %>
              <tr><td colspan="36">No favorites yet. Go to main home pageto start adding favorites!</td></tr>
            <% } %>
      </tbody>
    </table>


  <!-- Add New Button -->
  <!-- <button class="add-new-btn">Add New</button> -->

  
  <!-- Placeholder for future favorite items -->
  <p>Your favorites will show up here if you click the heart on the home page.</p>
</div>

</body>
</html>