<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>

        <style>
            body {
              font-family: Arial, sans-serif;
              background-color: #f4f4f4;
              margin: 0;
              padding: 0;
            }
            .welcome-label {
              position: absolute;
              top: 20px;
              left: 50%;
              transform: translateX(-50%);
              text-align: center;
              font-size: 2em;
              font-weight: bold;
              color: #333;
            }
            .forms-container {
              display: flex;
              justify-content: center;
              gap: 40px;
              margin-top: 100px;
            }
            form {
              width: 300px;
              padding: 20px;
              background-color: #ffffff;
              border: 1px solid #ddd;
              border-radius: 5px;
              box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            }
            form label {
              display: block;
              font-size: 1em;
              color: #333;
              margin-bottom: 10px;
            }
            form input[type="text"] {
              width: 100%;
              padding: 8px;
              margin: 10px 0;
              border: 1px solid #ddd;
              border-radius: 4px;
              box-sizing: border-box;
            }
            form button {
              width: 100%;
              padding: 10px;
              background-color: #4CAF50;
              color: white;
              border: none;
              border-radius: 4px;
              cursor: pointer;
              font-size: 1em;
            }
            form button:hover {
              background-color: #45a049;
            }
        </style>
    </head>
    
    <body>
        <label class="welcome-label">WELCOME TO HUMMUS!</label>
        
        <div class="forms-container">
            <form action="/searchUsername" method="get">
                <label>Please Enter Your Username and Password</label>
                <input type="text" id="searchUsername" name="searchUserName" placeholder="Username">
                <input type="text" id="searchPassword" name="searchPassword" placeholder="Password">
                <button type="submit">Login</button>
            </form>
            
            <form>
                <label>Don't Already Have an Account? Create One Here!</label>
                <button type="submit" id="createAccount" name="createAccount">Create Account</button>
            </form>
        </div>
    </body>
</html>
